{
  "compilerOptions": {
    "target": "es2016",                                  /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */
    "module": "commonjs",                                /* Specify what module code is generated. */
    "lib": ["ES6", "DOM"],                              /* Specify the library imports to generate. */
    "rootDir": "./src",                                  /* Specify the root folder within your source files. */
    "outDir": "./build",                                  /* Specify the output folder for generated files. */
    "strict": true,                                      /* Enable all strict type checking options. */
    "noImplicitAny": true,                               /* Warn on expressions and declarations with an implied 'any' type. */
    "esModuleInterop": true,                             /* Enable emit interoperability between CommonJS and ES Modules. */
    "skipLibCheck": true,                                /* Skip type checking of declaration files. */
    "forceConsistentCasingInFileNames": true,            /* Force consistent casing for identifiers. */

  },
  "include": [ 
    "./src"
  ],
  "exclude": [
    "node_modules",
    "spec",
    "build"
  ],
}
